def make_shirt(size, text):
    """Prints the size of the shirt and the message"""
    print(f"\nThis shirt is size {size}.")
    print(f"On the front, the writing sais {text.title()}.")

make_shirt('XL', 'filthy little hobbitses!')